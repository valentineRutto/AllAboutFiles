require 'spec_helper'

describe AllAboutFiles do
  it 'has a version number' do
    expect(AllAboutFiles::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
